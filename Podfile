# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-materialkit-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'MaterialKit', :git => 'https://github.com/faresbh/MaterialKit', :branch => "swift3"

  # Pods for ios-materialkit-demo

  target 'ios-materialkit-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-materialkit-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
